# Локальная конфигурация deploy.sh (не коммитить)
CR_REGISTRY=cr.yandex/crxxxxxxxx
VM_HOST=203.0.113.10
VM_USER=ubuntu
VM_DEPLOY_PATH=/opt/zemledel
SSH_KEY_PATH=~/.ssh/id_ed25519

# OAuth-токен или ключ для docker login в CR (yc iam create-token)
# YC_CR_TOKEN=

# Опционально переопределить тег (по умолчанию — git short sha)
# IMAGE_TAG=
